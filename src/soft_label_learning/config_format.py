from pathlib import Path

# Path to the processed UCI and Keel data for the synthetic data experiments.
path_uci_keel_data = Path()

# Path to the processed real-world data.
path_real_world_data = Path()

# Path to the output folder, where all figures are saved.
path_output = Path()

# Path to the repository itself, used to map to subdirectories.
path_repository = Path()
